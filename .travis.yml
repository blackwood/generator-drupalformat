language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-drupalformat' ]; then cd .. && eval "mv $currentfolder generator-drupalformat" && cd generator-drupalformat; fi

